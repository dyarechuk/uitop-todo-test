# Frontend Environment Variables (optional)
# For local development, uncomment the line below:
# VITE_API_URL=http://localhost:3001

# In Docker, the frontend uses /api (proxied through nginx to backend)

